# 系统端口配置说明

## 端口分配

| 服务名称 | 端口 | 说明 |
|---------|------|------|
| Redis | 6379 | 缓存服务 |
| Nacos | 6878 | **服务注册中心（自定义端口）** |
| 后台管理 | 7170 | PHP后台管理服务，需注册到Nacos |
| 前端API | 7180 | PHP前端API服务，需注册到Nacos |

## Nacos 配置说明

### 端口配置
- **标准端口**：8848（Nacos默认）
- **当前配置**：6878（自定义端口）

### 如何确认 Nacos 端口

1. 检查 Nacos 配置文件：
```bash
# 通常在以下位置
cat nacos/conf/application.properties | grep server.port
```

2. 查看 Nacos 启动日志：
```bash
cat nacos/logs/start.out | grep port
```

3. 测试 Nacos 是否在6878端口运行：
```bash
curl http://localhost:6878/nacos/
```

## 启动顺序

```
1. Redis (6379)
   ↓
2. Nacos (6878)
   ↓
3. Petrel 服务（多个JAR）
   ↓
4. PHP Web 服务
   - 7180 (前端API)
   - 7170 (后台管理)
   ↓
5. 注册到 Nacos
   - caipiao-frontend-api → 7180
   - caipiao-backend-admin → 7170
```

## 验证 Nacos 运行

### 方法1: 访问控制台
```bash
# 打开浏览器访问
http://localhost:6878/nacos/

# 默认用户名/密码
username: nacos
password: nacos
```

### 方法2: API测试
```bash
# 测试 Nacos API
curl http://localhost:6878/nacos/v1/console/health/readiness
```

### 方法3: 检查进程
```bash
# 查看 Nacos 进程
ps aux | grep nacos

# 查看端口占用
netstat -tlnp | grep 6878
# 或
lsof -i :6878
```

## 修改 Nacos 端口

如果需要修改 Nacos 端口，编辑配置文件：

```bash
# 编辑 Nacos 配置
vi nacos/conf/application.properties

# 修改端口（如果需要改回8848）
server.port=6878

# 重启 Nacos
sh nacos/bin/shutdown.sh
sh nacos/bin/startup.sh -m standalone
```

## 当前系统配置文件

### 1. nacos_register.php
```php
$nacosConfig = [
    'host' => '127.0.0.1',
    'port' => 6878,  // ✓ 已配置为6878
    'namespace' => 'public'
];
```

### 2. 启动并注册到Nacos.sh
```bash
NACOS_HOST="127.0.0.1"
NACOS_PORT="6878"  # ✓ 已配置为6878
```

### 3. 完整启动流程.sh
```bash
NACOS_PORT=6878  # ✓ 已配置为6878
```

## 测试命令

```bash
# 1. 测试 Nacos 是否运行在6878端口
curl http://localhost:6878/nacos/

# 2. 注册服务到 Nacos
php nacos_register.php register all

# 3. 查询已注册的服务
php nacos_register.php query

# 4. 通过 Nacos API 查询
curl "http://localhost:6878/nacos/v1/ns/instance/list?serviceName=caipiao-frontend-api"
curl "http://localhost:6878/nacos/v1/ns/instance/list?serviceName=caipiao-backend-admin"
```

## 常见问题

### Q1: Nacos 启动后访问不了？
**检查**：
1. 确认端口是6878还是8848
2. 查看启动日志：`cat nacos/logs/start.out`
3. 检查防火墙：`sudo ufw status`

### Q2: 服务注册失败？
**检查**：
1. Nacos 是否在运行
2. 端口配置是否正确（6878）
3. 网络连接是否正常

### Q3: 如何确认当前 Nacos 端口？
```bash
# 方法1：查看配置文件
grep "server.port" nacos/conf/application.properties

# 方法2：查看监听端口
netstat -tlnp | grep java

# 方法3：查看进程
ps aux | grep nacos | grep port
```

## 完整启动命令

```bash
# 按照标准流程启动所有服务
chmod +x 完整启动流程.sh
./完整启动流程.sh
```

这个脚本会：
1. ✓ 启动 Redis (6379)
2. ✓ 启动 Nacos (6878)
3. ✓ 启动 Petrel 服务
4. ✓ 启动 PHP 服务 (7180, 7170)
5. ✓ 注册服务到 Nacos (6878)

---

**重要提示**：
- ✅ Nacos 端口已配置为 **6878**
- ✅ 所有相关脚本已更新
- ✅ 服务注册会使用 6878 端口连接 Nacos
